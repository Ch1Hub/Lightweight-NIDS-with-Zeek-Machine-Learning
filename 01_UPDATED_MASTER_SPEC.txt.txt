IMPLEMENT THIS PROJECT EXACTLY.

PROJECT:
Lightweight academic IDS/IPS using Zeek + Machine Learning with two operating modes.

PRIMARY DATASET:
Use CIC-IDS2017.

TRAINING CLASSES:
- Benign
- DoS
- BruteForce
- PortScan

EXCLUDED CLASSES:
- DDoS
- Botnet
- WebAttack
- All other attack categories

ZERO-DAY EVALUATION:

Do NOT train on excluded attacks.

Use excluded attacks as unseen attacks during testing.

Recommended:

Primary unknown attack:
- DDoS

Optional unknown attacks:
- Botnet
- WebAttack

Expected behavior:

Unknown attack
→ Tier-1 identifies anomaly
→ Tier-2 fails to confidently classify
→ Output = Unknown


SYSTEM ARCHITECTURE:

Traffic
→ Zeek
→ Feature Extraction
→ Tier-1
→ Tier-2
→ Unknown Detection
→ Alert Engine


SYSTEM MUST SUPPORT TWO MODES


MODE 1: OFFLINE PCAP MODE

PCAP files
→ Zeek processing
→ Log generation
→ Feature extraction
→ Models
→ Output result


MODE 2: LIVE TRAFFIC MODE

Selected network interface
→ Packet capture
→ Time segmentation
→ Zeek processing
→ Feature extraction
→ Models
→ Output result


LIVE TRAFFIC WINDOWING REQUIREMENTS

For live traffic use:

Short window:
5 seconds

Aggregation window:
30 seconds

Requirements:

- Capture traffic continuously
- Aggregate features per window
- Generate a feature vector for every window
- Send each generated vector into the pipeline


GENERAL RULES

- Python only
- Modular architecture
- Every module independently testable
- Use virtual environments
- Use configuration JSON files
- Add logging to every module
- Save models using joblib
- Use SHAP for explainability
- Keep implementation lightweight
- Write clean code
- Include documentation
- Add exception handling
- Separate training and inference pipelines
